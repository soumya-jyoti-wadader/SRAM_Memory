[project]
name = "sram-memory"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cocotb>=2.0.1",
    "cocotb-tools>=0.1.0",
    "pytest>=9.0.3",
]
